# 一、“内外双路径自进化”优化思路

```text
内路径：运行轨迹 → 复盘审查 → Memory / Skill / Routing 经验沉淀 → 后续 planning 复用

外路径：运行轨迹 / 专家修正 / 人工标注 / 公开数据集
      → finetune_pool
      → 质量筛选
      → 数据增强/合成
      → 小规模 pilot 微调
      → 正式 formal 微调
      → 自动评估
      → 候选模型注册
      → shadow 验证
      → 人工确认后 active
```

注意，这里**不做 RL**。Hermes 文档中的 RL 路径在本项目中应替换为：

```text
受控微调训练闭环
```

Hermes 文档中“内路径”强调的是：任务完成后基于运行轨迹复盘，自动生成/更新 Skill，实现从“一次性执行”到“经验沉淀”；同时通过后台审查判断哪些内容值得进入 Memory 或 Skill。
文档中的“外路径”虽然讲 RL，但我们只借鉴其工程流程：数据构造、质量筛选、小规模实验、正式训练、自动评估、版本固化。

------

# 二、当前项目已有基础

根据最新仓库，当前已经具备这些基础：

| 能力               | 当前文件/模块                                     | 状态                                                         |
| ------------------ | ------------------------------------------------- | ------------------------------------------------------------ |
| 统一 CLI           | `ITD_agent/cli/main.py`                           | 已有 `run/review/train/state/export` 入口                    |
| 工作流总线         | `ITD_agent/orchestration/workflow.py`             | 已统一调度 run/review/train/state/export                     |
| 阶段分发           | `ITD_agent/orchestration/stage_runner.py`         | 已调度 adaptive/review/training/state/export                 |
| LLM 网关           | `ITD_agent/llm_gateway/gateway.py`                | 已有 planning、ROI、retrospective、fallback、audit           |
| Review 收口        | `ITD_agent/finetune_pool/review/review_runner.py` | 已读取 trajectory，写 memory/skill/finetune/routing/distillation |
| Skill draft 生成   | `finetune_pool/review/skill_reviewer.py`          | 已按 failure_family + min_support 生成 skill draft           |
| Skill 写入         | `finetune_pool/review/asset_writers.py`           | 已写 skill_records、drafts/SKILL.md、SQLite 记录             |
| Review 安全护栏    | `review_guardrails.py`                            | 已禁止 review 触发训练、更新权重、激活 hard skill、改 route map |
| Learning gate 雏形 | `learning_gate/evidence_gate.py`                  | 已能判断 success memory / failure pattern / finetune pool    |
| 参数空间定义       | `parameter_engine/expert_search_space.py`         | 已定义 HTC/Cascade/Mask Scoring/Mask2Former 搜索空间         |
| Scheduler context  | `planning/scheduler/context_builder.py`           | 已加载 memory、failure、finetune context，但缺 skill_context |
| 受控训练闭环       | `training_loop/training_runner.py`                | 已有 review assets、quality gate、pilot/formal、eval、guard、registry |

所以现在不是重做，而是**把已有模块串成闭环，并补缺口**。

------

# 三、总体落地方案

最终新增/修改模块如下：

```text
ITD_agent/
  cli/
    commands/
      evolve.py                         # 新增：受控自进化总命令

  orchestration/
    evolution_workflow.py               # 新增：run→review→train 可控串联

  learning_gate/
    event_builder.py                    # 新增：从 run/review/train 产物构建学习事件
    dispatcher.py                       # 新增：把学习事件分发到 memory/skill/finetune/routing
    skill_generation_policy.py          # 新增：Skill 生成触发策略

  skill_store/
    __init__.py                         # 新增
    query.py                            # 新增：读取已有 skill_records，不重复存储
    matcher.py                          # 新增：Skill 应用匹配，不叫触发
    usage.py                            # 新增：记录 Skill 被作为上下文使用后的效果

  parameter_engine/
    space_shrinker.py                   # 新增：基于 Skill 收缩专家模型参数空间

  training_loop/
    data_synthesis.py                   # 新增：受控数据增强/合成
    error_type_evaluator.py             # 新增：四类错误训练前后变化
    geometry_delta_evaluator.py         # 新增：冠幅、数量、覆盖度、密度等变化
    evolution_feedback.py               # 新增：训练结果反哺 memory/skill/routing

  planning/scheduler/
    context_builder.py                  # 修改：注入 skill_context
    runtime_scheduler.py                # 修改：使用 shrunk_search_space

  finetune_pool/review/
    skill_reviewer.py                   # 修改：扩展 Skill 生成类型和触发机制

  training_loop/
    training_runner.py                  # 修改：接入 data_synthesis、error_type_eval、feedback
```

------

# 四、内路径：Skill / Memory 经验沉淀闭环

## 4.1 内路径最终执行流程

```text
itd-agent run
  ↓
产生 run_summary / trajectory / state.sqlite
  ↓
itd-agent review
  ↓
review_runner 读取 trajectory
  ↓
learning_gate 判断是否值得沉淀
  ↓
skill_reviewer 生成 skill draft
  ↓
asset_writers 写入 skills/skill_records.jsonl + state.sqlite
  ↓
下一次 itd-agent run
  ↓
context_builder 读取 skill_context
  ↓
planning / llm_gateway / parameter_engine 参考 skill_context
```

------

## 4.2 Skill 生成触发机制

这里必须严格区分：

```text
Skill 生成触发机制 ≠ Skill 使用触发机制
```

Hermes 文档里提到的是“触发生成 Skill”的催促机制，而不是后续调用 Skill。

ITD_agent 中建议实现 4 类 Skill 生成触发。

------

### 4.2.1 重复失败触发

已有基础：`skill_reviewer.py` 当前已经根据 `failure_family` 和 `min_support_count` 生成 Skill draft。

保留并增强：

```yaml
skill_review:
  repeated_failure:
    enabled: true
    min_support_count: 3
```

生成类型：

```text
failure_interpretation_skill
expert_routing_skill
training_sample_selection_skill
fusion_guard_skill
```

------

### 4.2.2 周期性催促触发

参照 Hermes 的 `_skill_nudge_interval = 10`，本项目可配置为：

```yaml
skill_review:
  periodic_nudge:
    enabled: true
    every_n_trajectories: 10
```

含义：

```text
不是每 10 条 trajectory 强制生成 Skill
而是每累计 10 条 trajectory 检查是否存在值得沉淀的新模式
```

------

### 4.2.3 专家修正成功触发

当某专家模型在某类失败上稳定优于主模型：

```yaml
skill_review:
  expert_success:
    enabled: true
    min_support_count: 2
    min_score_improvement: 0.03
```

生成：

```text
expert_routing_skill
parameter_adjustment_skill
training_sample_selection_skill
```

------

### 4.2.4 融合/回滚触发

当专家模型介入后多次被拒绝、回滚或导致指标下降：

```yaml
skill_review:
  fusion_guard:
    enabled: true
    min_rejected_refinement_count: 2
```

生成：

```text
fusion_guard_skill
failure_interpretation_skill
```

------

## 4.3 修改 `skill_reviewer.py`

当前已有：

```python
def build_class_level_skill_records(...):
    ...
```

建议改成：

```python
def build_class_level_skill_records(
    *,
    review_run_id: str,
    source_run_id: str,
    contexts: list[Any],
    cfg: dict[str, Any],
) -> list[dict[str, Any]]:
    records = []
    records.extend(_build_repeated_failure_skills(...))
    records.extend(_build_periodic_nudge_skills(...))
    records.extend(_build_expert_success_skills(...))
    records.extend(_build_fusion_guard_skills(...))
    return _dedupe_skill_records(records)
```

保留原有 `min_support_count` 逻辑，不推翻。

------

## 4.4 Skill 类型定义

最终只保留 6 类，不要复杂化：

| Skill 类型                        | 作用                   | 主要应用环节                        |
| --------------------------------- | ---------------------- | ----------------------------------- |
| `failure_interpretation_skill`    | 解释失败模式           | evaluation_analysis、review         |
| `expert_routing_skill`            | 指导专家模型选择       | planning/scheduler                  |
| `roi_selection_skill`             | 指导 ROI 排序          | ROI candidate、local refine         |
| `parameter_adjustment_skill`      | 指导参数空间收缩       | parameter_engine、runtime_scheduler |
| `fusion_guard_skill`              | 指导接受/回滚          | evolution/fusion、expert assessment |
| `training_sample_selection_skill` | 指导样本入池和训练目标 | finetune_pool、training_loop        |

------

## 4.5 Skill 内容格式

生成的 Skill 不应该只是普通 Markdown，也不应该是 Python 脚本。
建议每个 Skill 是结构化 YAML/JSON，同时可以生成一个人类可读 `SKILL.md`。

标准结构：

```yaml
skill_id: skill_small_crown_recall_shadow_dense_canopy_v001
skill_type: training_sample_selection_skill
name: 小冠幅漏检样本筛选经验
status: draft
version: review.1

source:
  source_run_ids: [...]
  source_trajectory_ids: [...]
  review_run_id: ...

generation_trigger:
  trigger_type: repeated_failure
  failure_family: small_crown_recall
  support_count: 5
  reason: repeated small crown false-negative cases

applicable_context:
  mainline_profile: A_DOM_ONLY
  failure_family: small_crown_recall
  scene_conditions:
    crown_size: small
    canopy_density: dense
    shadow_ratio: high
    texture_contrast: low

problem_pattern:
  description: 主模型在小冠幅、低纹理、阴影边缘区域稳定漏检
  observed_metrics:
    false_negative_ratio: high
    tree_count_error_ratio: high

recommended_action:
  mode: readonly_suggestion

  planning:
    prefer_expert_family: small_crown_recall

  roi_selection:
    prioritize_tags:
      - small_crown
      - shadow_edge
      - dense_canopy

  parameter_space:
    shrink:
      score_thr:
        direction: decrease
      min_area_px:
        direction: decrease
      tile_overlap:
        direction: increase

  finetune_pool:
    sample_priority: high
    target_model_role: expert_model
    target_expert_family: small_crown_recall

  training:
    recommended_augmentation:
      - small_crown_copy_paste
      - scale_jitter
      - brightness_shadow_perturbation

safety_constraints:
  can_modify_active_model: false
  can_update_active_route_map: false
  can_start_training: false
  requires_human_review_for_activation: true

evidence_summary:
  support_count: 5
  improved_cases: 3
  failed_cases: 2
```

------

## 4.6 新增 `skill_store/query.py`

不要新造一套存储系统，复用当前已有：

```text
output_dir/skills/skill_records.jsonl
output_dir/skills/drafts/
state.sqlite skill_records 表
```

新增查询层即可。

```python
from __future__ import annotations

import json
import sqlite3
from pathlib import Path
from typing import Any


def load_skill_records(
    *,
    db_path: str | Path | None = None,
    review_output_dir: str | Path | None = None,
    statuses: list[str] | None = None,
    limit: int = 50,
) -> list[dict[str, Any]]:
    statuses = statuses or ["draft", "shadow", "active"]
    if db_path and Path(db_path).exists():
        return _load_from_sqlite(Path(db_path), statuses=statuses, limit=limit)
    if review_output_dir:
        return _load_from_jsonl(Path(review_output_dir) / "skills" / "skill_records.jsonl", statuses=statuses, limit=limit)
    return []


def _load_from_sqlite(db_path: Path, *, statuses: list[str], limit: int) -> list[dict[str, Any]]:
    conn = sqlite3.connect(str(db_path))
    conn.row_factory = sqlite3.Row
    rows = conn.execute(
        """
        SELECT *
        FROM skill_records
        WHERE status IN ({})
        ORDER BY created_at DESC
        LIMIT ?
        """.format(",".join("?" for _ in statuses)),
        [*statuses, limit],
    ).fetchall()
    conn.close()
    return [dict(row) for row in rows]


def _load_from_jsonl(path: Path, *, statuses: list[str], limit: int) -> list[dict[str, Any]]:
    if not path.exists():
        return []
    records = []
    for line in path.read_text(encoding="utf-8").splitlines():
        if not line.strip():
            continue
        item = json.loads(line)
        if item.get("status") in statuses:
            records.append(item)
    return records[-limit:]
```

------

## 4.7 新增 `skill_store/matcher.py`

注意命名：这叫**应用匹配**，不要叫触发机制。

```python
from __future__ import annotations

from typing import Any


def match_skill_context(
    *,
    skills: list[dict[str, Any]],
    scene_profile: dict[str, Any],
    evaluation_metrics: dict[str, Any],
    roi_assessment: dict[str, Any],
    failure_pattern_context: list[dict[str, Any]],
    max_items: int = 5,
) -> dict[str, Any]:
    matched = []
    active_failure_families = _collect_failure_families(roi_assessment, failure_pattern_context)

    for skill in skills:
        trigger_conditions = _safe_json(skill.get("trigger_conditions_json") or skill.get("trigger_conditions") or {})
        evidence = _safe_json(skill.get("evidence_summary_json") or skill.get("evidence_summary") or {})
        recommended = _safe_json(skill.get("recommended_action_json") or skill.get("recommended_action") or {})

        failure_family = trigger_conditions.get("failure_family") or evidence.get("failure_family")
        score = 0.0
        reasons = []

        if failure_family and failure_family in active_failure_families:
            score += 0.6
            reasons.append(f"matched_failure_family:{failure_family}")

        if skill.get("status") == "active":
            score += 0.2
        elif skill.get("status") == "shadow":
            score += 0.1

        support_count = int(evidence.get("support_count") or 0)
        if support_count >= 3:
            score += 0.1

        if score > 0:
            matched.append(
                {
                    "skill_id": skill.get("skill_id"),
                    "skill_type": skill.get("skill_type"),
                    "status": skill.get("status"),
                    "score": round(score, 4),
                    "matched_reasons": reasons,
                    "recommended_action": recommended,
                    "evidence_summary": evidence,
                }
            )

    matched = sorted(matched, key=lambda x: x["score"], reverse=True)[:max_items]
    return {
        "matched_skill_count": len(matched),
        "matched_skills": matched,
        "application_mode": "context_only_readonly_suggestion",
    }


def _collect_failure_families(roi_assessment: dict[str, Any], failure_patterns: list[dict[str, Any]]) -> set[str]:
    out = set()
    for key in ["failure_family", "target_failure_family"]:
        if roi_assessment.get(key):
            out.add(str(roi_assessment[key]))
    for item in failure_patterns:
        if item.get("failure_family"):
            out.add(str(item["failure_family"]))
    return out


def _safe_json(value: Any) -> dict[str, Any]:
    import json
    if isinstance(value, dict):
        return value
    if isinstance(value, str) and value.strip():
        try:
            return json.loads(value)
        except json.JSONDecodeError:
            return {}
    return {}
```

------

## 4.8 修改 `planning/scheduler/context_builder.py`

在 `build_scheduler_context()` 中加入：

```python
from ITD_agent.skill_store.query import load_skill_records
from ITD_agent.skill_store.matcher import match_skill_context
```

在返回前构建：

```python
skill_records = load_skill_records(
    db_path=runtime_cfg.get("state_db_path"),
    review_output_dir=runtime_cfg.get("review_output_dir"),
    statuses=["draft", "shadow", "active"],
    limit=50,
) if allow_memory_context else []

skill_context = match_skill_context(
    skills=skill_records,
    scene_profile=scene_profile,
    evaluation_metrics=metrics,
    roi_assessment=runtime_cfg.get("_roi_assessment") or {},
    failure_pattern_context=load_recent_failure_patterns(limit=recent_failure_limit) if allow_memory_context else [],
)
```

返回字典新增：

```python
"skill_context": skill_context,
```

当前 `context_builder.py` 已经加载 memory、failure、execution trace、scene similar memory、finetune pool，但还没有 skill_context。这里是最关键的接入点。

------

# 五、parameter_engine：参数空间收缩落地方案

## 5.1 它主要用在哪？

`parameter_engine` 应用于：

```text
专家模型推理前
ROI 局部细化前
planning/scheduler 生成专家模型计划时
小规模参数试验或 pilot search 时
```

它不直接训练模型，也不直接改权重。

当前 `expert_search_space.py` 已经定义了专家模型合法参数空间，包括 `score_thr`、`merge_iou_thr`、`min_area_px`、`mask_thr_binary`、`height_support_gate`、`tile_size`、`tile_overlap` 等。

------

## 5.2 新增 `parameter_engine/space_shrinker.py`

```python
from __future__ import annotations

from copy import deepcopy
from typing import Any


def shrink_search_space_with_skill(
    *,
    base_space: dict[str, Any],
    skill_context: dict[str, Any],
    failure_family: str | None = None,
) -> dict[str, Any]:
    shrunk = deepcopy(base_space)
    matched_skills = skill_context.get("matched_skills") or []

    for skill in matched_skills:
        action = skill.get("recommended_action") or {}
        parameter_space = action.get("parameter_space") or {}
        shrink_rules = parameter_space.get("shrink") or {}

        for param_name, rule in shrink_rules.items():
            _apply_shrink_rule(shrunk, param_name, rule)

    shrunk["_shrink_metadata"] = {
        "source": "skill_context",
        "matched_skill_ids": [s.get("skill_id") for s in matched_skills],
        "failure_family": failure_family,
    }
    return shrunk


def _apply_shrink_rule(space: dict[str, Any], param_name: str, rule: dict[str, Any]) -> None:
    for section_name in ["decision_params", "deployment_params"]:
        section = space.get(section_name) or {}
        if param_name not in section:
            continue

        spec = section[param_name]
        direction = rule.get("direction")

        if "range" in spec and isinstance(spec["range"], list) and len(spec["range"]) == 2:
            lo, hi = float(spec["range"][0]), float(spec["range"][1])
            mid = (lo + hi) / 2.0
            if direction == "decrease":
                spec["range"] = [lo, mid]
            elif direction == "increase":
                spec["range"] = [mid, hi]
            section[param_name] = spec

        elif "values" in spec and isinstance(spec["values"], list):
            values = spec["values"]
            if not values:
                continue
            if direction == "decrease":
                spec["values"] = values[: max(1, len(values) // 2)]
            elif direction == "increase":
                spec["values"] = values[len(values) // 2 :]
            section[param_name] = spec
```

------

## 5.3 与 planning/scheduler 配合

在专家模型计划生成处：

```python
from ITD_agent.parameter_engine.expert_search_space import get_expert_model_search_space
from ITD_agent.parameter_engine.space_shrinker import shrink_search_space_with_skill

base_space = get_expert_model_search_space(expert_algorithm)
shrunk_space = shrink_search_space_with_skill(
    base_space=base_space,
    skill_context=scheduler_context.get("skill_context") or {},
    failure_family=target_failure_family,
)
```

然后把 `shrunk_space` 放入：

```python
expert_plan["parameter_search_space"] = shrunk_space
```

最终由 `runtime_scheduler` 生成受控参数。

------

# 六、外路径：受控微调训练闭环

## 6.1 外路径最终流程

```text
review assets
  ↓
finetune_pool samples
  ↓
sample_quality_gate
  ↓
data_synthesis / augmentation
  ↓
dataset_bundle
  ↓
pilot training
  ↓
post_train_evaluation
  ↓
replay_guard / geometry_guard
  ↓
formal training
  ↓
自动评估
  ↓
model_registry
  ↓
promotion suggestion
  ↓
training feedback
  ↓
memory / skill / routing candidate
```

当前 `training_runner.py` 已经有这条链路的大部分内容，但缺 `data_synthesis`、更完整的 `error_type_delta / geometry_delta` 和训练反哺。

------

## 6.2 新增 `training_loop/data_synthesis.py`

第一版不做生成式大模型，不做 RL，只做受控数据增强。

```python
from __future__ import annotations

import json
from pathlib import Path
from typing import Any


def synthesize_training_samples(
    *,
    accepted_samples: list[dict[str, Any]],
    cfg: dict[str, Any],
    output_dir: str | Path,
) -> dict[str, Any]:
    if not bool(cfg.get("enabled", False)):
        return {"synthetic_samples": [], "report": {"enabled": False}}

    max_ratio = float(cfg.get("max_synthetic_ratio", 0.3))
    max_count = int(len(accepted_samples) * max_ratio)

    synthetic_samples = []
    for sample in accepted_samples[:max_count]:
        synthetic = _build_synthetic_sample_stub(sample, cfg)
        synthetic_samples.append(synthetic)

    root = Path(output_dir) / "dataset_bundle" / "synthesis"
    root.mkdir(parents=True, exist_ok=True)

    samples_path = root / "synthetic_samples.jsonl"
    with samples_path.open("w", encoding="utf-8") as f:
        for item in synthetic_samples:
            f.write(json.dumps(item, ensure_ascii=False) + "\n")

    report = {
        "enabled": True,
        "source_sample_count": len(accepted_samples),
        "synthetic_sample_count": len(synthetic_samples),
        "max_synthetic_ratio": max_ratio,
        "policy": "train_only_no_val_test_replay",
    }
    (root / "synthesis_report.json").write_text(json.dumps(report, indent=2, ensure_ascii=False), encoding="utf-8")
    return {"synthetic_samples": synthetic_samples, "report": report}


def _build_synthetic_sample_stub(sample: dict[str, Any], cfg: dict[str, Any]) -> dict[str, Any]:
    metadata = dict(sample.get("metadata") or {})
    failure_category = sample.get("failure_category") or metadata.get("failure_category")
    strategies = _strategies_for_failure(str(failure_category))
    return {
        **sample,
        "sample_id": f"synthetic_{sample.get('sample_id')}",
        "source_sample_id": sample.get("sample_id"),
        "source_type": "synthetic_augmentation",
        "label_status": "synthetic",
        "split_policy": "train_only",
        "augmentation_strategies": strategies,
        "ready_for_training": True,
    }


def _strategies_for_failure(failure_category: str) -> list[str]:
    mapping = {
        "false_negative": ["small_crown_copy_paste", "scale_jitter", "brightness_shadow_perturbation"],
        "under_segmentation": ["dense_canopy_crop", "boundary_jitter", "local_contrast_jitter"],
        "over_segmentation": ["large_crown_context_crop", "boundary_smoothing"],
        "false_positive": ["hard_negative_crop", "shadow_background_sampling"],
    }
    return mapping.get(failure_category, ["safe_color_jitter"])
```

后续再把 stub 替换为真实图像增强实现。

------

## 6.3 修改 `training_runner.py`

在 `materialize_training_dataset_bundle()` 之前插入：

```python
from ITD_agent.training_loop.data_synthesis import synthesize_training_samples

synthesis = synthesize_training_samples(
    accepted_samples=quality["accepted_samples"],
    cfg=((cfg.get("training") or {}).get("data_synthesis") or {}),
    output_dir=output_dir,
)

training_samples = quality["accepted_samples"] + synthesis["synthetic_samples"]
```

然后：

```python
dataset_bundle = materialize_training_dataset_bundle(
    accepted_samples=training_samples,
    replay_samples=replay_samples,
    ...
)
```

------

## 6.4 新增 `training_loop/error_type_evaluator.py`

```python
from __future__ import annotations
from typing import Any


def compute_error_type_delta(
    *,
    baseline_errors: dict[str, Any],
    candidate_errors: dict[str, Any],
) -> dict[str, Any]:
    keys = [
        "under_segmentation",
        "over_segmentation",
        "false_positive",
        "false_negative",
        "small_crown_recall",
        "large_crown_split",
    ]
    delta = {}
    for key in keys:
        before = _to_float(baseline_errors.get(key))
        after = _to_float(candidate_errors.get(key))
        if before is None or after is None:
            delta[key + "_delta"] = None
        else:
            delta[key + "_delta"] = after - before
    return {
        "status": "computed",
        "delta": delta,
        "interpretation": _interpret(delta),
    }


def _to_float(value: Any) -> float | None:
    try:
        if value is None:
            return None
        return float(value)
    except Exception:
        return None


def _interpret(delta: dict[str, Any]) -> dict[str, str]:
    out = {}
    for key, value in delta.items():
        if value is None:
            out[key] = "not_available"
        elif value < 0:
            out[key] = "improved"
        elif value > 0:
            out[key] = "regressed"
        else:
            out[key] = "unchanged"
    return out
```

------

## 6.5 新增 `training_loop/geometry_delta_evaluator.py`

```python
from __future__ import annotations
from typing import Any


def compute_geometry_delta(
    *,
    baseline_metrics: dict[str, Any],
    candidate_metrics: dict[str, Any],
) -> dict[str, Any]:
    keys = [
        "tree_count_error_ratio",
        "mean_crown_width_error_ratio",
        "closure_error_abs",
        "density_error_abs",
    ]
    delta = {}
    for key in keys:
        before = _float_or_none(baseline_metrics.get(key))
        after = _float_or_none(candidate_metrics.get(key))
        delta[key + "_delta"] = None if before is None or after is None else after - before
    return {
        "status": "computed",
        "delta": delta,
        "lower_is_better": keys,
    }


def _float_or_none(value: Any) -> float | None:
    try:
        if value is None:
            return None
        return float(value)
    except Exception:
        return None
```

------

## 6.6 新增 `training_loop/evolution_feedback.py`

```python
from __future__ import annotations

import json
from pathlib import Path
from typing import Any


def write_training_evolution_feedback(
    *,
    training_summary: dict[str, Any],
    output_dir: str | Path,
) -> dict[str, Any]:
    root = Path(output_dir) / "evolution_feedback"
    root.mkdir(parents=True, exist_ok=True)

    promotion = training_summary.get("promotion_decision") or {}
    sample_quality = training_summary.get("sample_quality_report") or {}
    model_version = training_summary.get("model_version") or {}

    feedback = {
        "feedback_type": "training_evolution_feedback",
        "training_status": (training_summary.get("training_result") or {}).get("status"),
        "promotion_decision": promotion.get("decision"),
        "model_version_id": model_version.get("model_version_id"),
        "accepted_samples": sample_quality.get("accepted_count"),
        "recommended_memory_write": True,
        "recommended_skill_update": promotion.get("decision") in {"promote_to_shadow"},
        "recommended_routing_review": promotion.get("decision") in {"promote_to_shadow", "keep_candidate"},
    }

    path = root / "training_evolution_feedback.json"
    path.write_text(json.dumps(feedback, indent=2, ensure_ascii=False), encoding="utf-8")
    return {"feedback_path": str(path), "feedback": feedback}
```

------

# 七、新增受控进化总命令

当前已有 `run/review/train`，但缺一个能受控串联的总命令。

## 7.1 新增 `ITD_agent/cli/commands/evolve.py`

```python
from __future__ import annotations

from argparse import ArgumentParser, Namespace
from typing import Any

from ITD_agent.orchestration.evolution_workflow import run_controlled_evolution


def register(subparsers: Any) -> None:
    parser: ArgumentParser = subparsers.add_parser("evolve", help="Run controlled self-evolution workflow.")
    parser.add_argument("--config", required=True)
    parser.set_defaults(handler=handle)


def handle(args: Namespace) -> dict[str, Any]:
    return run_controlled_evolution(args.config)
```

修改 `cli/main.py`：

```python
from ITD_agent.cli.commands import export, review, run, state, train, evolve
...
evolve.register(subparsers)
```

------

## 7.2 新增 `orchestration/evolution_workflow.py`

```python
from __future__ import annotations

import json
from pathlib import Path
from typing import Any

import yaml

from ITD_agent.orchestration import workflow
from ITD_agent.learning_gate.event_builder import (
    build_learning_events_from_run_result,
    build_learning_events_from_review_result,
    build_learning_events_from_training_result,
)
from ITD_agent.learning_gate.dispatcher import dispatch_learning_events


def run_controlled_evolution(config_path: str | Path) -> dict[str, Any]:
    cfg = yaml.safe_load(Path(config_path).read_text(encoding="utf-8"))
    closed_loop = cfg.get("closed_loop") or {}

    result: dict[str, Any] = {
        "mode": "controlled_self_evolution",
        "config_path": str(config_path),
        "run": None,
        "review": None,
        "training": None,
        "learning_events": [],
    }

    run_result = workflow.run(cfg["run_config"])
    result["run"] = run_result

    run_events = build_learning_events_from_run_result(run_result)
    dispatch_report = dispatch_learning_events(
        events=run_events,
        cfg=cfg.get("learning_gate") or {},
        output_dir=closed_loop.get("output_dir") or "outputs/controlled_evolution",
    )
    result["learning_events"].append({"stage": "post_run", "report": dispatch_report})

    if bool(closed_loop.get("review_after_run", True)):
        review_result = workflow.review(cfg["review_config"])
        result["review"] = review_result
        review_events = build_learning_events_from_review_result(review_result)
        dispatch_report = dispatch_learning_events(
            events=review_events,
            cfg=cfg.get("learning_gate") or {},
            output_dir=closed_loop.get("output_dir") or "outputs/controlled_evolution",
        )
        result["learning_events"].append({"stage": "post_review", "report": dispatch_report})

    if bool(closed_loop.get("train_after_review", False)):
        training_result = workflow.train(cfg["training_config"])
        result["training"] = training_result
        training_events = build_learning_events_from_training_result(training_result)
        dispatch_report = dispatch_learning_events(
            events=training_events,
            cfg=cfg.get("learning_gate") or {},
            output_dir=closed_loop.get("output_dir") or "outputs/controlled_evolution",
        )
        result["learning_events"].append({"stage": "post_training", "report": dispatch_report})

    return result
```

默认不自动训练：

```yaml
closed_loop:
  review_after_run: true
  train_after_review: false
  promote_model_to_active: false
```

------

# 八、配置示例

新增：

```text
configs/examples/controlled_self_evolution.yaml
```

内容：

```yaml
closed_loop:
  output_dir: outputs/controlled_self_evolution
  review_after_run: true
  train_after_review: false
  promote_model_to_active: false
  activate_skill_policy: false

run_config: configs/examples/dom_only_closed_loop.yaml
review_config: configs/examples/review_closed_loop.yaml
training_config: configs/examples/controlled_training.yaml

learning_gate:
  enabled: true
  min_score_delta_for_success_memory: 0.03
  min_repeat_count_for_failure_pattern: 2
  allow_write_memory: true
  allow_write_skill_draft: true
  allow_write_finetune_pool: true
  allow_active_skill_policy: false

skill_review:
  repeated_failure:
    enabled: true
    min_support_count: 3
  periodic_nudge:
    enabled: true
    every_n_trajectories: 10
  expert_success:
    enabled: true
    min_support_count: 2
    min_score_improvement: 0.03
  fusion_guard:
    enabled: true
    min_rejected_refinement_count: 2

training:
  data_synthesis:
    enabled: true
    max_synthetic_ratio: 0.3

guardrails:
  allow_training_trigger: false
  allow_weight_update: false
  allow_model_promotion: false
  allow_active_skill_policy: false
  allow_routing_policy_update: false
```

------

# 九、验收标准

## 9.1 CLI 验收

必须通过：

```bash
python -m ITD_agent.cli.main --help
```

能看到：

```text
run
review
train
state
export
evolve
```

执行：

```bash
itd-agent evolve --config configs/examples/controlled_self_evolution.yaml
```

默认行为：

```text
会执行 run
会执行 review
不会执行 train，除非 train_after_review=true
不会激活 skill
不会替换 active model
不会更新 active route_map
```

------

## 9.2 内路径验收

执行：

```bash
itd-agent run --config ...
itd-agent review --config ...
```

必须产出：

```text
outputs/<run_id>/trajectories/*.json
outputs/<review_id>/memory/memory_records.jsonl
outputs/<review_id>/skills/skill_records.jsonl
outputs/<review_id>/skills/drafts/*/SKILL.md
state.sqlite 中存在 skill_records / review_events
```

Skill 记录必须包含：

```text
skill_id
skill_type
status=draft
trigger_conditions
recommended_action
evidence_summary
safety_constraints
source_trajectory_ids
```

------

## 9.3 Skill 生成触发验收

构造 3 条相同 failure_family 的 trajectory，运行 review 后：

```text
必须生成对应 skill draft
```

例如：

```text
failure_family=small_crown_recall
→ training_sample_selection_skill
```

构造专家成功案例：

```text
expert_success_count >= 2
score_improvement >= 0.03
```

必须生成：

```text
expert_routing_skill 或 parameter_adjustment_skill
```

构造专家回滚案例：

```text
rejected_refinement_count >= 2
```

必须生成：

```text
fusion_guard_skill
```

------

## 9.4 Skill 安全验收

review 配置中如果设置：

```yaml
guardrails:
  allow_active_skill_policy: true
```

必须报错或被强制阻断。

`review_guardrails.py` 已有类似设计，应保持：

```text
review 只能写 skill draft
不能写 active hard policy
不能启动训练
不能更新模型权重
不能更新 route_map
```



------

## 9.5 Skill 应用匹配验收

在下一次运行中，`planning/scheduler/context_builder.py` 生成的 scheduler context 必须包含：

```json
{
  "skill_context": {
    "matched_skill_count": 1,
    "matched_skills": [...]
  }
}
```

同时 `main_model_plan` 或 `expert_model_plan` 中应能看到：

```text
scheduler_context.skill_context
```

但不能出现：

```text
Skill 直接修改 active route_map
Skill 直接启动训练
Skill 直接替换模型
```

------

## 9.6 parameter_engine 验收

给定：

```text
expert_algorithm=mmdet_htc
skill_context 中建议 score_thr decrease, min_area_px decrease
```

执行：

```python
base_space = get_expert_model_search_space("mmdet_htc")
shrunk = shrink_search_space_with_skill(base_space=base_space, skill_context=skill_context)
```

必须满足：

```text
shrunk 仍然在 expert_search_space 的合法范围内
score_thr 范围被收缩
min_area_px 候选被收缩
不会产生非法参数
不会直接写 runtime config
```

------

## 9.7 外路径训练验收

当 `train_after_review=true` 或单独执行：

```bash
itd-agent train --config configs/examples/controlled_training.yaml
```

必须产出：

```text
finetune_bundle/finetune_dataset_bundle.json
finetune_bundle/sample_quality_report.json
dataset_bundle/dataset_card.json
dataset_bundle/synthesis/synthesis_report.json
training_jobs/*/training_plan.json
training_jobs/*/command.sh
evaluation/baseline_eval.json
evaluation/candidate_eval.json
evaluation/delta_eval.json
replay_guard/replay_guard_report.json
model_registry/model_versions.jsonl 或 registration_skipped.json
reports/training_summary.json
```

如果 `runner.execute_training=false`：

```text
training_result.status 必须是 skipped
不得误报 completed
```

如果 `runner.execute_training=true` 且训练成功：

```text
必须找到 best_checkpoint_path
必须写 model_versions.jsonl
promotion_decision 不得 promote_to_active
```

------

## 9.8 数据合成/增强验收

当：

```yaml
training:
  data_synthesis:
    enabled: true
    max_synthetic_ratio: 0.3
```

必须产出：

```text
dataset_bundle/synthesis/synthetic_samples.jsonl
dataset_bundle/synthesis/synthesis_report.json
```

并满足：

```text
synthetic_sample_count <= accepted_sample_count * max_synthetic_ratio
synthetic 样本只能进入 train
不能进入 val/test/replay
每条 synthetic 样本必须有 source_sample_id
每条 synthetic 样本必须标记 label_status=synthetic
```

------

## 9.9 自动评估验收

训练后必须输出：

```text
error_type_delta.json
geometry_delta.json
```

至少包含：

```text
under_segmentation_delta
over_segmentation_delta
false_positive_delta
false_negative_delta
tree_count_error_ratio_delta
mean_crown_width_error_ratio_delta
closure_error_abs_delta
density_error_abs_delta
```

如果缺少必要输入，必须明确写：

```json
{
  "status": "skipped",
  "reason": "missing_baseline_or_candidate_error_metrics"
}
```

不能静默跳过。

------

## 9.10 安全闭环验收

全流程中必须始终满足：

```text
LLM 只能给建议
Skill 只能给建议
Review 不能触发训练
Training 不能自动替换 active model
Training 不能自动更新 active route_map
Promotion 只能到 candidate/shadow
active 需要人工确认
```

------

# 十、最终通过标准

只有满足以下 7 条，才算“内外双路径受控自进化”完善完成：

1. `itd-agent evolve` 可以受控串联 `run → review → train`，但默认不自动训练。
2. `review` 能基于 trajectory 自动生成 memory、skill draft、finetune sample、routing candidate。
3. Skill 生成触发机制包括重复失败、周期性催促、专家成功、融合回滚。
4. Skill 能作为 `skill_context` 注入 planning，但不能直接执行高风险动作。
5. `parameter_engine` 能根据 Skill 建议收缩专家模型参数空间，但不越界、不直接写配置。
6. `training_loop` 能完成质量筛选、数据增强/合成、pilot/formal、自动评估、replay guard、model registry。
7. 新模型只能注册为 candidate/shadow，不能自动替换 active 模型。

