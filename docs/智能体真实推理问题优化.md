

**必须走项目正式 CLI，不走 scripts；必须复用 input_layer / output_layer；新增 COCO/PNG 链路但不能破坏 DOM/TIF 链路；数据集级运行，不是单图临时脚本；configs 不允许被运行污染。**

```text
请基于当前仓库实际源码和 ZS-TreeSeg 官方提供的方法，落地一条 COCO/PNG 数据集真实推理链路。注意：本任务必须严格遵守当前项目已有正式流程，不允许绕开 CLI、input_layer、workflow、output_layer，也不允许把 scripts/ 当成正式入口。

背景说明：
1. 当前项目已有正式 CLI 模块：ITD_agent/cli/main.py，通过 argparse 注册 run、evolve、review、train、state、export 等正式子命令。
2. 当前项目正式命令流程是：
   ITD_agent.cli.main
   → ITD_agent.cli.commands.<command>
   → ITD_agent.orchestration.workflow
   → 具体 workflow / pipeline
   → input_layer / segmentation / evaluation_analysis / output_layer
3. 因此本次 COCO/PNG 全流程不能使用 scripts/run_coco_png_full_real.py 作为正式入口。
4. 如果之前已经实现或计划实现 scripts/run_coco_png_full_real.py，请迁移到正式 CLI + workflow 链路，并删除该 scripts 正式入口。
5. scripts/ 模块不能承担正式推理、训练、评估、智能体闭环入口。正式流程必须全部通过 ITD_agent/cli 注册。
6. ZS-TreeSeg 官方方法是 PIL.Image / NumPy / SegFormer / Cellpose-SAM 风格，不要求 GeoTIFF。当前项目已有 DOM/TIF geospatial-backend，不能破坏。COCO/PNG 应作为新增 image-backend 链路实现。
7. ZS-TreeSeg 论文/官方说明中，核心思想是先做 canopy semantic segmentation，再用 Cellpose-SAM 做 crown instance segmentation；本次 COCO/PNG 后端应按该方法实现 PNG/JPG 输入的主模型推理。可参考官方公开说明：ZS-TreeSeg 是一个 zero-shot tree crown instance segmentation 框架，将 canopy semantic segmentation 与 Cellpose-SAM instance segmentation 结合。:contentReference[oaicite:0]{index=0}

一、必须遵守的项目边界

1. 不允许破坏现有 DOM/TIF geospatial-backend。
   - 不要修改 runtime_entrypoints/semantic_prior_segformer.py 的 rasterio/TIF 逻辑。
   - 不要修改 runtime_entrypoints/segmentation_legacy_cellpose_sam.py 的 rasterio/TIF 逻辑。
   - 不要让现有 ITD_agent.orchestration.orchestrator 依赖新的 COCO/PNG pipeline。
   - 不要改变 ITD_agent/segmentation/executor.py 中 geospatial/TIF 路线的行为。
   - 原有 DOM/TIF 命令和配置加载必须继续可用。

2. 不允许绕开 input_layer。
   - COCO/PNG 链路必须使用 input_type=coco_dataset。
   - 必须调用 input_layer.adapters.normalize_agent_runtime_config() 完成输入标准化和非法字段检查。
   - 不允许引入或保留 DEM/CHM/DSM/xiaoban/terrain/canopy/surface/mainline 等字段。
   - 不允许自己另写一套输入规范绕开项目输入边界。

3. 不允许绕开 output_layer。
   - 最终输出必须构造 output_layer.contracts.FinalTreeCrownResult。
   - 必须调用 output_layer.publisher.publish_final_tree_crown_outputs()。
   - 最终输出目录必须符合现有项目规范：
     output/<任务命名>/
       results/
       masks/
       visualization/
       report/
   - working/intermediate 中间产物可以放在 output/<任务命名>/working/ 下，但最终给用户看的结果必须由 output_layer 发布。

4. 不允许污染 configs/。
   - configs/ 只保留固定模板。
   - 不允许每次运行复制、新建、改写 configs/ 下的 YAML。
   - 本次运行解析后的配置只能写到 output/<任务命名>/working/resolved_input_config.yaml。
   - 运行期动态配置、预测结果、评估结果、调度结果不得写回 configs/。

5. 不允许把 scripts/ 作为正式业务入口。
   - scripts/ 如保留，只能是开发维护工具、数据检查工具、迁移工具。
   - 正式推理、训练、评估、智能体闭环必须通过 ITD_agent/cli 注册。
   - 本次新增正式入口必须是 ITD_agent.cli.main 下的子命令。
   
6. 专家模型必须真实推理；如果环境、config 或 checkpoint 缺失，要明确报错，不允许 mock。
7. 新增代码必须独立于 geospatial backend，避免影响 DOM/TIF 正常运行。

二、正式 CLI 入口设计

请新增正式 CLI 子命令：

coco-png-infer

正式执行命令应为：

python -u -m ITD_agent.cli.main coco-png-infer \
  --template configs/examples/coco_dataset_inference.yaml \
  --dataset-root /home/xth/forest_agent_project/data/Dataset_4/Validation_set \
  --image-root /home/xth/forest_agent_project/data/Dataset_4/Validation_set/images \
  --annotation /home/xth/forest_agent_project/data/Dataset_4/Validation_set/annotation/instances.json \
  --output-dir outputs/coco_png_full_real \
  --run-name coco_png_full_real \
  --max-images 5 \
  --max-expert-rounds 1

如果项目安装后存在 itd-agent console script，也可以补充验证：

itd-agent coco-png-infer \
  --template configs/examples/coco_dataset_inference.yaml \
  --dataset-root /home/xth/forest_agent_project/data/Dataset_4/Validation_set \
  --image-root /home/xth/forest_agent_project/data/Dataset_4/Validation_set/images \
  --annotation /home/xth/forest_agent_project/data/Dataset_4/Validation_set/annotation/instances.json \
  --output-dir outputs/coco_png_full_real \
  --run-name coco_png_full_real \
  --max-images 5 \
  --max-expert-rounds 1

参数含义：
- --max-images：数据集采样参数，用于限制本次最多处理多少张图片。不提供时默认处理 annotation 中全部 images；首次真实验证建议设为 5。
- --max-expert-rounds：专家模型闭环次数控制参数，表示主模型之后最多执行几轮专家模型修正。第一版默认为 1。

三、需要新增或修改的文件

1. 修改 ITD_agent/cli/main.py

当前 main.py 已注册 run/evolve/review/train/state/export。请新增 coco_png command 注册。

要求：
- 导入 ITD_agent.cli.commands.coco_png。
- 调用 coco_png.register(subparsers)。
- 保持现有命令不变。

2. 新增 ITD_agent/cli/commands/coco_png.py

职责：
- 只负责 argparse 参数解析。
- 注册 coco-png-infer 子命令。
- 不允许在 CLI 文件里写主模型推理、评估、专家模型、融合、输出发布等业务逻辑。
- handler 必须调用 ITD_agent.orchestration.workflow.coco_png_infer(...)。

参数：
--template
--dataset-root
--image-root
--annotation
--output-dir
--run-name
--split
--max-images
--image-id
--image-name
--max-expert-rounds
--device

建议结构参考现有 ITD_agent/cli/commands/run.py 和 evolve.py：
- register(subparsers)
- handle(args)

3. 修改 ITD_agent/orchestration/workflow.py

要求：
- 新增 coco_png_infer(...) 函数。
- 在 run_workflow() 的 commands 映射中加入：
  "coco-png-infer": coco_png_infer
- coco_png_infer 只负责构建 command/context/result 结构，并调用：
  ITD_agent.orchestration.coco_png_pipeline.run_coco_png_pipeline(...)
- 保持 run/evolve/evolve-infer/adaptive-inference/review/train/state/export 行为不变。

4. 新增 ITD_agent/orchestration/coco_png_pipeline.py

这是 COCO/PNG 数据集真实推理业务编排层。

职责：
1. 读取固定 template YAML。
2. 从 CLI 参数注入 dataset_root / image_root / annotation / output_dir / run_name / split / max_images / image_id / image_name / max_expert_rounds / device。
3. 将 input_type 设置为 coco_dataset。
4. 调用 input_layer.adapters.normalize_agent_runtime_config() 做输入标准化和非法字段检查。
5. 保存 output_dir/working/resolved_input_config.yaml。
6. 读取 COCO annotation 中的 images 列表。
7. 按 max-images / image-id / image-name 筛选待推理图片。
8. 写 output_dir/working/selected_images.json。
9. 批量调用 PNG 主模型推理入口。
10. 汇总 output_dir/working/main_model/main_prediction_coco.json。
11. 调用 COCO evaluator 生成 output_dir/working/evaluation/main_metrics.json。
12. 根据 main_metrics.json 中 dominant_error_type 选择专家模型。
13. 调用 PNG expert executor 对需要处理的图片执行专家模型真实推理。
14. 汇总 output_dir/working/expert_model/expert_prediction_coco.json。
15. 调用 png_fusion 得到 output_dir/working/fusion/fused_prediction_coco.json。
16. 再评估 fused_prediction_coco.json，生成 output_dir/working/evaluation/fused_metrics.json。
17. 构造 FinalTreeCrownResult。
18. 调用 publish_final_tree_crown_outputs() 发布最终结果。
19. 写 output_dir/final_summary.json。

注意：
- working/ 是过程产物目录。
- results/、masks/、visualization/、report/ 必须由 output_layer 统一发布。
- 不允许手写最终输出结构绕过 output_layer。

5. 新增 runtime_entrypoints/zstreeseg_png_main_model.py

职责：
- 单张 PNG/JPG 主模型推理入口。
- 使用 PIL.Image.open(image).convert("RGB") 读取图像。
- 使用本地 SegFormer 模型生成 semantic_mask。
- 使用 Cellpose-SAM 生成 instance_mask。
- 不经过 TIF，不调用 rasterio。
- 输出到：
  output_dir/working/main_model/per_image/<image_id>/
    semantic_mask.png
    semantic_mask.npy
    instance_mask.png
    instance_mask.npy
    prediction.json
    summary.json
- prediction.json 使用 COCO prediction 格式。

边界：
- 只负责单张图像主模型推理。
- 不负责数据集扫描。
- 不负责专家模型。
- 不负责最终 output_layer 发布。
- 不负责写 configs/。

6. 新增 ITD_agent/evaluation_analysis/coco_instance_evaluator.py

职责：
- 输入 COCO GT annotation 和 prediction_coco.json。
- 对数据集或筛选图片做 IoU matching。
- 输出 metrics JSON。
- 第一版可以先实现实例级 IoU matching，不强制完整 pycocotools AP。
- 至少输出：
  precision
  recall
  f1
  mean_iou
  tp
  fp
  fn
  false_positive_count
  false_negative_count
  low_iou_count
  under_segmentation_score
  over_segmentation_score
  false_positive_score
  false_negative_score
  boundary_quality_score
  dominant_error_type

7. 新增 ITD_agent/segmentation/png_expert_executor.py

职责：
- 读取 template 中 expert_models.default_templates 或当前配置中已有专家模型字段。
- 支持 htc、cascade_mask_rcnn、mask2former、maskdino。
- 直接对 PNG/JPG 执行专家模型真实推理。
- 输出：
  output_dir/working/expert_model/expert_prediction_coco.json
  output_dir/working/expert_model/selected_expert.json
- 如果 mmdetection 环境、config_file、checkpoint_file、driver 或依赖缺失，必须明确报错。
- 不允许 fallback 成 mock 结果。
- 不允许调用 geospatial/TIF executor。

8. 新增 ITD_agent/segmentation/png_fusion.py

职责：
- 输入 main_prediction_coco.json、expert_prediction_coco.json、dominant_error_type。
- 输出：
  output_dir/working/fusion/fused_prediction_coco.json
  output_dir/working/fusion/fusion_summary.json
- 第一版可使用规则融合，但必须记录融合决策。
- 融合策略建议：
  under_segmentation：优先采用 HTC 等专家结果替换粘连区域。
  over_segmentation：优先采用 Mask2Former 或主模型稳定结果，抑制碎片。
  false_positive_cleanup：用 Cascade Mask R-CNN 或语义一致性删除误检。
  missed_crown_recall：用 MaskDINO 补充主模型漏检。
  boundary_quality：选择 mask 边界质量更高的一方。

四、输出结构要求

最终输出必须是：

output/<任务命名>/
├── results/
├── masks/
├── visualization/
└── report/

同时允许保留：

output/<任务命名>/
├── working/
│   ├── resolved_input_config.yaml
│   ├── selected_images.json
│   ├── main_model/
│   │   ├── per_image/
│   │   └── main_prediction_coco.json
│   ├── expert_model/
│   │   ├── selected_expert.json
│   │   └── expert_prediction_coco.json
│   ├── fusion/
│   │   ├── fused_prediction_coco.json
│   │   └── fusion_summary.json
│   └── evaluation/
│       ├── main_metrics.json
│       └── fused_metrics.json
├── results/
│   └── coco_predictions.json
├── masks/
├── visualization/
├── report/
│   ├── evaluation_report.md
│   └── evaluation_report.json
├── final_result_bundle.json
├── final_report.md
├── final_report.json
└── final_summary.json

其中：
- working/ 是过程产物。
- results/、masks/、visualization/、report/ 必须由 output_layer.publisher.publish_final_tree_crown_outputs() 生成。
- results/coco_predictions.json 应该对应最终融合后的 fused_prediction_coco.json，而不是主模型未融合结果，除非专家模型未运行或失败。

五、关于 scripts/ 模块的处理

如果仓库中已经存在或本次之前已经新增：

scripts/run_coco_png_full_real.py

请按以下方式处理：
1. 将参数解析迁移到 ITD_agent/cli/commands/coco_png.py。
2. 将业务逻辑迁移到 ITD_agent/orchestration/coco_png_pipeline.py。
3. 删除 scripts/run_coco_png_full_real.py。
4. 不允许保留 scripts 作为正式业务入口。
5. scripts/ 如保留，只能作为开发维护工具，不得作为用户执行主入口。

六、验收命令

1. CLI help 检查：

python -m ITD_agent.cli.main --help

必须看到 coco-png-infer。

2. 子命令 help 检查：

python -m ITD_agent.cli.main coco-png-infer --help

必须看到完整参数。

3. 小规模真实推理：

python -u -m ITD_agent.cli.main coco-png-infer \
  --template configs/examples/coco_dataset_inference.yaml \
  --dataset-root /home/xth/forest_agent_project/data/Dataset_4/Validation_set \
  --image-root /home/xth/forest_agent_project/data/Dataset_4/Validation_set/images \
  --annotation /home/xth/forest_agent_project/data/Dataset_4/Validation_set/annotation/instances.json \
  --output-dir outputs/coco_png_full_real \
  --run-name coco_png_full_real \
  --max-images 5 \
  --max-expert-rounds 1

七、验收标准

1. configs/ 不新增任何运行配置。
2. 不存在 scripts/run_coco_png_full_real.py 作为正式入口。
3. python -m ITD_agent.cli.main --help 能看到 coco-png-infer。
4. python -m ITD_agent.cli.main coco-png-infer --help 能看到完整参数。
5. output/coco_png_full_real/working/resolved_input_config.yaml 存在。
6. output/coco_png_full_real/working/selected_images.json 存在。
7. output/coco_png_full_real/working/main_model/main_prediction_coco.json 存在。
8. output/coco_png_full_real/working/evaluation/main_metrics.json 存在。
9. 如果专家模型环境齐全，output/coco_png_full_real/working/expert_model/expert_prediction_coco.json 存在。
10. output/coco_png_full_real/working/fusion/fused_prediction_coco.json 存在。
11. output/coco_png_full_real/results/ 存在。
12. output/coco_png_full_real/masks/ 存在。
13. output/coco_png_full_real/visualization/ 存在。
14. output/coco_png_full_real/report/ 存在。
15. output/coco_png_full_real/results/coco_predictions.json 存在。
16. output/coco_png_full_real/report/evaluation_report.md 存在。
17. output/coco_png_full_real/report/evaluation_report.json 存在。
18. output/coco_png_full_real/final_result_bundle.json 存在。
19. output/coco_png_full_real/final_summary.json 存在。
20. 原有 DOM/TIF 正式命令不得受影响，例如：
    python -m ITD_agent.cli.main run --config configs/examples/dom_image_inference.yaml
    至少能完成配置加载，不得因为 COCO/PNG 新链路报错。

八、最终架构要求

最终链路必须是：

ITD_agent.cli.main
→ ITD_agent.cli.commands.coco_png
→ ITD_agent.orchestration.workflow.coco_png_infer
→ ITD_agent.orchestration.coco_png_pipeline.run_coco_png_pipeline
→ input_layer.adapters.normalize_agent_runtime_config
→ runtime_entrypoints.zstreeseg_png_main_model
→ evaluation_analysis.coco_instance_evaluator
→ segmentation.png_expert_executor
→ segmentation.png_fusion
→ output_layer.publisher.publish_final_tree_crown_outputs

禁止链路：
scripts/run_coco_png_full_real.py
→ 直接执行业务逻辑

禁止行为：
- 不允许绕开 input_layer。
- 不允许绕开 output_layer。
- 不允许写 configs/generated 或在 configs/ 下生成运行配置。
- 不允许把 COCO/PNG pixel-space 输出伪装成真实 GIS/SHP 输出。
- 不允许破坏 DOM/TIF 原有流程。
```

